import 'package:flutter/material.dart';

class CompositionWidgets extends StatelessWidget {
  const CompositionWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Hellooooooooo');
  }
}
